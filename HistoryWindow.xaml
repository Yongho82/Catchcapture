<Window x:Class="CatchCapture.HistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:utils="clr-namespace:CatchCapture.Utilities"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen" ResizeMode="CanResize"
        MinHeight="300" Height="820" MinWidth="1100" Width="1430"
        Title="히스토리 - 캐치캡처"
        Background="Transparent" WindowStyle="None" AllowsTransparency="True">
    
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="60" 
                      ResizeBorderThickness="8" 
                      CornerRadius="12" 
                      GlassFrameThickness="0" />
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <!-- Sidebar Button Style (Reusing from NoteExplorer) -->
        <Style x:Key="SidebarItemStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="{DynamicResource ThemeForeground}"/> 
            <Setter Property="FontSize" Value="13"/> <!-- Increased Font Size -->
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="brd" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="brd" Property="Background" Value="{DynamicResource ThemeSidebarButtonHoverBackground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- GridViewColumnHeader Style for Dark Mode -->
        <Style TargetType="GridViewColumnHeader">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource ThemeForeground}"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ThemeBorder}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GridViewColumnHeader">
                        <Grid>
                            <Border x:Name="HeaderBorder" BorderBrush="{DynamicResource ThemeBorder}" BorderThickness="0,0,1,1" Padding="{TemplateBinding Padding}">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Color="#15FFFFFF" Offset="0.0"/>
                                        <GradientStop Color="Transparent" Offset="0.1"/>
                                        <GradientStop Color="#10000000" Offset="1.0"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="Center" RecognizesAccessKey="True"/>
                            </Border>
                            <!-- Gripper for resizing -->
                            <Thumb x:Name="PART_HeaderGripper" HorizontalAlignment="Right" Width="10" Margin="0,0,-5,0" Cursor="SizeWE">
                                <Thumb.Template>
                                    <ControlTemplate TargetType="Thumb">
                                        <Border Background="Transparent" Width="10"/>
                                    </ControlTemplate>
                                </Thumb.Template>
                            </Thumb>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <utils:FirstLineConverter x:Key="FirstLineConverter"/>

        <!-- Dark Mode Support for Filters -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource ThemeBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource ThemeForeground}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ThemeBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5,3"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="DatePicker">
            <Setter Property="Foreground" Value="{DynamicResource ThemeForeground}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ThemeBorder}"/>
        </Style>



        <Style TargetType="DatePickerTextBox">
            <Setter Property="Background" Value="{DynamicResource ThemeBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource ThemeForeground}"/>
        </Style>



        <!-- Card Mode Template -->
        <DataTemplate x:Key="HistoryCardTemplate">
            <Border x:Name="CardBorder" Background="{DynamicResource ThemeSidebarButtonPressedBackground}" CornerRadius="10" Margin="5" Width="200" Height="250" BorderThickness="2" BorderBrush="Transparent" UseLayoutRounding="True" SnapsToDevicePixels="True">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="150"/> <!-- Image -->
                        <RowDefinition Height="*"/>   <!-- Info -->
                    </Grid.RowDefinitions>
                    
                    <!-- Image Border with matching CornerRadius - Using ImageBrush for reliable clipping -->
                    <Border Grid.Row="0" CornerRadius="8,8,0,0">
                        <Border.Background>
                            <ImageBrush ImageSource="{Binding Thumbnail}" Stretch="UniformToFill" RenderOptions.BitmapScalingMode="HighQuality"/>
                        </Border.Background>
                    </Border>
                    
                    <!-- Fallback/Overlay for image area -->
                    <Border Grid.Row="0" Background="#08000000" CornerRadius="8,8,0,0" IsHitTestVisible="False"/>
                    
                    <!-- Selection CheckBox -->
                    <CheckBox x:Name="ChkSelect" Grid.Row="0" IsChecked="{Binding IsSelected}" Click="ChkItem_Click"
                              HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10" 
                              Style="{DynamicResource {x:Type CheckBox}}" Visibility="Collapsed"/>

                    <Grid Grid.Row="1" Margin="12,10,12,10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/> <!-- Title -->
                            <RowDefinition Height="*"/>    <!-- Date & Icons -->
                            <RowDefinition Height="Auto"/> <!-- App Info -->
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="{Binding FileName}" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource ThemeForeground}" TextTrimming="CharacterEllipsis"/>
                        
                        <Grid Grid.Row="1" VerticalAlignment="Center" Margin="0,2">
                            <TextBlock Text="{Binding FormattedDate}" FontSize="11" Opacity="0.6" Foreground="{DynamicResource ThemeForeground}" VerticalAlignment="Center"/>
                            
                            <!-- Action Icons (Date Row) -->
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <!-- Favorite Toggle -->
                                <Button Click="BtnFavorite_Click" Background="Transparent" BorderThickness="0" Cursor="Hand" Padding="2">
                                    <Grid>
                                        <Path x:Name="StarFilled" Data="M12,17.27L18.18,21L16.54,13.97L22,9.24L14.81,8.62L12,2L9.19,8.62L2,9.24L7.45,13.97L5.82,21L12,17.27Z" 
                                              Fill="#FFD700" Width="13" Height="13" Stretch="Uniform" Visibility="Collapsed"/>
                                        <Path x:Name="StarOutline" Data="M12,15.39L8.24,17.66L9.23,13.38L5.91,10.5L10.29,10.13L12,6.09L13.71,10.13L18.09,10.5L14.77,13.38L15.76,17.66L12,15.39M22,9.24L14.81,8.62L12,2L9.19,8.62L2,9.24L7.45,13.97L5.82,21L12,17.27L18.18,21L16.54,13.97L22,9.24Z" 
                                              Fill="{DynamicResource ThemeForeground}" Opacity="0.3" Width="13" Height="13" Stretch="Uniform" Visibility="Collapsed"/>
                                    </Grid>
                                </Button>
                                <!-- Pin Toggle -->
                                <Button Click="BtnPin_Click" Background="Transparent" BorderThickness="0" Cursor="Hand" Padding="2">
                                    <Grid>
                                        <Path x:Name="PinFilled" Data="M16,12V4H17V2H7V4H8V12L6,14V16H11.2V22H12.8V16H18V14L16,12Z" 
                                              Fill="#FF5252" Width="13" Height="13" Stretch="Uniform" Visibility="Collapsed"/>
                                        <Path x:Name="PinOutline" Data="M16,12V4H17V2H7V4H8V12L6,14V16H11.2V22H12.8V16H18V14L16,12M14.2,14H9.8L10,12.2V4H14V12.2L14.2,14Z" 
                                              Fill="{DynamicResource ThemeForeground}" Opacity="0.3" Width="13" Height="13" Stretch="Uniform" Visibility="Collapsed"/>
                                    </Grid>
                                </Button>
                                <!-- Delete Action (Only on hover) -->
                                <Button x:Name="BtnDeleteAction" Click="BtnDeleteItem_Click" Background="Transparent" BorderThickness="0" Cursor="Hand" Padding="2" Visibility="Collapsed" ToolTip="삭제">
                                    <Path Data="M9,3V4H4V6H5V19A2,2 0 0,0 7,21H17A2,2 0 0,0 19,19V6H20V4H15V3H9M7,6H17V19H7V6M9,8V17H11V8H9M13,8V17H15V8H13Z" 
                                          Fill="{DynamicResource ThemeForeground}" Opacity="0.4" Width="13" Height="13" Stretch="Uniform"/>
                                </Button>
                            </StackPanel>
                        </Grid>

                        <TextBlock Grid.Row="2" Text="{Binding SourceApp}" FontSize="11" Opacity="0.5" Foreground="{DynamicResource ThemeForeground}" TextTrimming="CharacterEllipsis"/>
                    </Grid>
                </Grid>
            </Border>
            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding DataContext.IsSelectionActive, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                    <Setter TargetName="ChkSelect" Property="Visibility" Value="Visible"/>
                    <Setter TargetName="StarOutline" Property="Visibility" Value="Visible"/>
                    <Setter TargetName="PinOutline" Property="Visibility" Value="Visible"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListViewItem}}" Value="True">
                    <Setter TargetName="CardBorder" Property="BorderBrush" Value="#8E2DE2"/>
                    <Setter TargetName="ChkSelect" Property="Visibility" Value="Visible"/>
                    <Setter TargetName="StarOutline" Property="Visibility" Value="Visible"/>
                    <Setter TargetName="PinOutline" Property="Visibility" Value="Visible"/>
                </DataTrigger>
                <Trigger SourceName="CardBorder" Property="IsMouseOver" Value="True">
                    <Setter TargetName="BtnDeleteAction" Property="Visibility" Value="Visible"/>
                    <Setter TargetName="StarOutline" Property="Visibility" Value="Visible"/>
                    <Setter TargetName="PinOutline" Property="Visibility" Value="Visible"/>
                    <Setter TargetName="ChkSelect" Property="Visibility" Value="Visible"/>
                </Trigger>
                <DataTrigger Binding="{Binding IsFavorite}" Value="True">
                    <Setter TargetName="StarFilled" Property="Visibility" Value="Visible"/>
                    <Setter TargetName="StarOutline" Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding IsPinned}" Value="True">
                    <Setter TargetName="PinFilled" Property="Visibility" Value="Visible"/>
                    <Setter TargetName="PinOutline" Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>
    </Window.Resources>

    <Border Background="{DynamicResource ThemeBackground}" CornerRadius="12" BorderThickness="1" BorderBrush="{DynamicResource ThemeWindowBorder}">
        <Border.Effect>
            <DropShadowEffect BlurRadius="25" Color="Black" Opacity="0.1" ShadowDepth="5"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Action Bar (Search & Filter) -->
                <RowDefinition Height="*"/>    <!-- Main Content -->
                <RowDefinition Height="Auto"/> <!-- Status Bar -->
            </Grid.RowDefinitions>

            <!-- Top Action Bar -->
            <Border Grid.Row="0" Background="{DynamicResource ThemeTitleBackground}" CornerRadius="12,12,0,0" Padding="25,12,20,12" BorderThickness="0,0,0,1" BorderBrush="{DynamicResource ThemeBorder}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/> <!-- Title/Icon -->
                        <ColumnDefinition Width="*"/>    <!-- Search Box -->
                        <ColumnDefinition Width="Auto"/> <!-- Window Controls -->
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Image Source="/icons/histroy_note.png" Width="22" Height="22" Margin="0,0,12,0" VerticalAlignment="Center"/>
                        <TextBlock x:Name="TxtTitleBarTitle" Text="히스토리" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource ThemeTitleForeground}" VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Search & Advanced Filter -->
                    <StackPanel Grid.Column="1" WindowChrome.IsHitTestVisibleInChrome="True" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Border x:Name="SearchBorder" Background="#20000000" BorderBrush="{DynamicResource ThemeBorder}" BorderThickness="1" CornerRadius="18" Height="36" Width="450">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsKeyboardFocusWithin, ElementName=TxtSearch}" Value="True">
                                            <Setter Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                                            <Setter Property="Background" Value="#30000000"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Grid>
                                <TextBox x:Name="TxtSearch" KeyDown="TxtSearch_KeyDown" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" FontSize="13" 
                                         Foreground="{DynamicResource ThemeTitleForeground}" CaretBrush="{DynamicResource ThemeTitleForeground}" Margin="15,0,35,0" Padding="0"/>
                                <TextBlock x:Name="TxtSearchPlaceholder" Text="파일명, 파일내용, 앱 이름 등 검색..." IsHitTestVisible="False" Foreground="{DynamicResource ThemeTitleForeground}" Opacity="0.4" VerticalAlignment="Center" Margin="17,0" FontSize="13">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Text, ElementName=TxtSearch}" Value="">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <Button x:Name="BtnSearch" Click="BtnSearch_Click" HorizontalAlignment="Right" Width="36" Background="Transparent" BorderThickness="0" Cursor="Hand">
                                    <TextBlock Text="🔍" FontSize="14" Foreground="{DynamicResource ThemeTitleForeground}"/>
                                </Button>
                            </Grid>
                        </Border>


                        
                        <!-- Advanced Filter Button -->
                        <Button x:Name="BtnAdvancedFilter" Click="BtnAdvancedFilter_Click" Width="36" Height="36" Margin="8,0,0,0" Background="Transparent" BorderThickness="0" Cursor="Hand" ToolTip="상세 필터">
                            <Path Data="M14,12L19.88,5.12C20.19,4.77 20.21,4.24 19.93,3.87C19.66,3.5 19.2,3.29 18.73,3.29H5.27C4.8,3.29 4.34,3.5 4.06,3.87C3.79,4.24 3.81,4.77 4.12,5.12L10,12V18C10,18.33 10.16,18.64 10.43,18.83L13.43,21.08C13.59,21.19 13.79,21.25 14,21.25C14.15,21.25 14.3,21.21 14.43,21.13C14.78,20.93 15,20.57 15,20.17V12L14,12Z" 
                                  Fill="{DynamicResource ThemeTitleForeground}" Stretch="Uniform" Width="18" Height="18" Opacity="0.8"/>
                        </Button>

                        <Popup x:Name="PopupFilter" PlacementTarget="{Binding ElementName=BtnAdvancedFilter}" StaysOpen="False" AllowsTransparency="True">
                            <Border Background="{DynamicResource ThemeBackground}" BorderBrush="{DynamicResource ThemeBorder}" BorderThickness="1" CornerRadius="8" Padding="15" Width="300">
                                <StackPanel>
                                    <TextBlock Text="상세 필터" FontWeight="Bold" Margin="0,0,0,10" Foreground="{DynamicResource ThemeForeground}"/>
                                    <TextBlock Text="날짜 필터" FontSize="11" Opacity="0.7" Margin="0,0,0,5" Foreground="{DynamicResource ThemeForeground}"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                        <DatePicker x:Name="DateFrom" Width="120" Margin="0,0,5,0"/>
                                        <TextBlock Text="~" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="{DynamicResource ThemeForeground}"/>
                                        <DatePicker x:Name="DateTo" Width="120"/>
                                    </StackPanel>
                                    <TextBlock Text="파일명" FontSize="11" Opacity="0.7" Margin="0,0,0,5" Foreground="{DynamicResource ThemeForeground}"/>
                                    <TextBox x:Name="TxtFilterFileName" Margin="0,0,0,10" Height="28" Padding="5,4"/>
                                    <TextBlock Text="파일 종류" FontSize="11" Opacity="0.7" Margin="0,0,0,5" Foreground="{DynamicResource ThemeForeground}"/>
                                    <ComboBox x:Name="CmbFileType" Height="28" Margin="0,0,0,15">
                                        <ComboBoxItem Content="전체" IsSelected="True"/>
                                        <ComboBoxItem Content="이미지 (JPG, PNG, BMP, GIF, WEBP)"/>
                                        <ComboBoxItem Content="미디어 (MP4, MP3)"/>
                                    </ComboBox>
                                    <Button x:Name="BtnApplyAdvancedFilter" Click="BtnApplyAdvancedFilter_Click" Content="필터 적용" Height="32" Background="#1A73E8" Foreground="White" BorderThickness="0" Cursor="Hand">
                                        <Button.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="6"/>
                                            </Style>
                                        </Button.Resources>
                                    </Button>
                                </StackPanel>
                            </Border>
                        </Popup>
                    </StackPanel>

                    <!-- Window Buttons -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" WindowChrome.IsHitTestVisibleInChrome="True">
                        <Button x:Name="BtnMinimize" Width="36" Height="36" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="BtnMinimize_Click">
                            <Path Data="M20,14H4V10H20V14Z" Fill="{DynamicResource ThemeTitleForeground}" Opacity="0.8" Stretch="Uniform" Width="14" Height="14"/>
                        </Button>
                        <Button x:Name="BtnClose" Width="36" Height="36" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="BtnClose_Click">
                            <Path Data="M19,6.41L17.59,5L12,10.59L6.41,5L5,6.41L10.59,12L5,17.59L6.41,19L12,13.41L17.59,19L19,17.59L13.41,12L19,6.41Z" 
                                  Fill="{DynamicResource ThemeTitleForeground}" Opacity="0.8" Stretch="Uniform" Width="16" Height="16"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Main Content Grid (3 Columns) -->
            <Grid Grid.Row="1" x:Name="MainGrid">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220"/> <!-- Sidebar -->
                    <ColumnDefinition Width="*"/>   <!-- List (Calculated: 875) -->
                    <ColumnDefinition Width="Auto"/> <!-- Splitter -->
                    <ColumnDefinition Width="331" MinWidth="250" MaxWidth="1000"/> <!-- Preview Pane -->
                </Grid.ColumnDefinitions>

                <!-- Sidebar (1st Column) -->
                <Border Grid.Column="0" Background="{DynamicResource ThemeSidebarButtonBackground}" BorderThickness="0,0,1,0" BorderBrush="{DynamicResource ThemeBorder}">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="15,20,5,0">
                                <Grid Margin="15,0,10,10">
                                    <TextBlock x:Name="TxtTotalListLabel" Text="전체 리스트" Foreground="{DynamicResource ThemeForeground}" Opacity="0.9" FontSize="12" FontWeight="Bold" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                    <Button x:Name="BtnSync" Click="BtnSync_Click" HorizontalAlignment="Right" VerticalAlignment="Center" 
                                            Background="Transparent" BorderThickness="0" Cursor="Hand" ToolTip="최신 기록 가져오기">
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <Viewbox Width="12" Height="12" Margin="0,0,4,0">
                                                <Path Data="M12,18A6,6 0 0,1 6,12C6,11 6.25,10.03 6.7,9.2L5.24,7.74C4.46,8.97 4,10.43 4,12A8,8 0 0,0 12,20V23L16,19L12,15V18M12,2V5L8,9L12,13V10A6,6 0 0,1 18,12C18,13 17.75,13.97 17.3,14.8L18.76,16.26C19.54,15.03 20,13.57 20,12A8,8 0 0,0 12,4V1Z" 
                                                      Fill="{DynamicResource ThemeForeground}" Opacity="0.6"/>
                                            </Viewbox>
                                            <TextBlock x:Name="TxtSync" Text="동기화" FontSize="12" Foreground="{DynamicResource ThemeForeground}" Opacity="0.6" VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Opacity" Value="0.7"/>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Opacity" Value="1"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </Grid>
                            <Button x:Name="BtnAll" Style="{StaticResource SidebarItemStyle}" Tag="All" Click="BtnFilter_Click">
                                <Grid Width="140">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="📋" Margin="0,0,12,0" Foreground="{DynamicResource ThemeForeground}"/>
                                        <TextBlock x:Name="TxtAllFilter" Text="전체 기록" Foreground="{DynamicResource ThemeForeground}"/>
                                    </StackPanel>
                                    <TextBlock x:Name="TxtCountAll" Text="0" HorizontalAlignment="Right" Opacity="0.7" VerticalAlignment="Center" Foreground="{DynamicResource ThemeForeground}"/>
                                </Grid>
                            </Button>
                            
                            <Separator Background="{DynamicResource ThemeBorder}" Margin="10,15"/>
                            
                            <TextBlock x:Name="TxtPeriodLabel" Text="기간별" Foreground="{DynamicResource ThemeForeground}" Opacity="0.9" FontSize="12" FontWeight="Bold" Margin="15,0,0,10"/>
                            <Button x:Name="BtnRecent7" Style="{StaticResource SidebarItemStyle}" Tag="Recent7" Click="BtnFilter_Click">
                                <Grid Width="140">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="🕐" Margin="0,0,12,0" Opacity="0.9" Foreground="{DynamicResource ThemeForeground}"/>
                                        <TextBlock x:Name="TxtRecent7Filter" Text="최근 7일" Foreground="{DynamicResource ThemeForeground}"/>
                                    </StackPanel>
                                    <TextBlock x:Name="TxtCountRecent7" Text="0" HorizontalAlignment="Right" Opacity="0.7" VerticalAlignment="Center" Foreground="{DynamicResource ThemeForeground}"/>
                                </Grid>
                            </Button>
                            <Button x:Name="BtnRecent30" Style="{StaticResource SidebarItemStyle}" Tag="Recent30" Click="BtnFilter_Click">
                                <Grid Width="140">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="🕒" Margin="0,0,12,0" Opacity="0.9" Foreground="{DynamicResource ThemeForeground}"/>
                                        <TextBlock x:Name="TxtRecent30Filter" Text="최근 30일" Foreground="{DynamicResource ThemeForeground}"/>
                                    </StackPanel>
                                    <TextBlock x:Name="TxtCountRecent30" Text="0" HorizontalAlignment="Right" Opacity="0.7" VerticalAlignment="Center" Foreground="{DynamicResource ThemeForeground}"/>
                                </Grid>
                            </Button>
                            <Button x:Name="BtnRecent3Months" Style="{StaticResource SidebarItemStyle}" Tag="Recent3Months" Click="BtnFilter_Click">
                                <Grid Width="140">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="🗓️" Margin="0,0,12,0" Opacity="0.9" Foreground="{DynamicResource ThemeForeground}"/>
                                        <TextBlock x:Name="TxtRecent3MonthsFilter" Text="최근 3개월" Foreground="{DynamicResource ThemeForeground}"/>
                                    </StackPanel>
                                    <TextBlock x:Name="TxtCountRecent3Months" Text="0" HorizontalAlignment="Right" Opacity="0.7" VerticalAlignment="Center" Foreground="{DynamicResource ThemeForeground}"/>
                                </Grid>
                            </Button>
                            <Button x:Name="BtnRecent6Months" Style="{StaticResource SidebarItemStyle}" Tag="Recent6Months" Click="BtnFilter_Click">
                                <Grid Width="140">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="🗓️" Margin="0,0,12,0" Opacity="0.9" Foreground="{DynamicResource ThemeForeground}"/>
                                        <TextBlock x:Name="TxtRecent6MonthsFilter" Text="최근 6개월" Foreground="{DynamicResource ThemeForeground}"/>
                                    </StackPanel>
                                    <TextBlock x:Name="TxtCountRecent6Months" Text="0" HorizontalAlignment="Right" Opacity="0.7" VerticalAlignment="Center" Foreground="{DynamicResource ThemeForeground}"/>
                                </Grid>
                            </Button>

                            <Separator Background="{DynamicResource ThemeBorder}" Margin="10,15"/>

                            <Button x:Name="BtnPinned" Style="{StaticResource SidebarItemStyle}" Tag="Pinned" Click="BtnFilter_Click">
                                <Grid Width="140">
                                    <StackPanel Orientation="Horizontal">
                                        <Path Data="M16,12V4H17V2H7V4H8V12L6,14V16H11.2V22H12.8V16H18V14L16,12Z" 
                                              Fill="#FF5252" Width="18" Height="18" Stretch="Uniform" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                        <TextBlock x:Name="TxtPinnedFilter" Text="고정됨" Foreground="{DynamicResource ThemeForeground}" VerticalAlignment="Center"/>
                                    </StackPanel>
                                    <TextBlock x:Name="TxtCountPinned" Text="0" HorizontalAlignment="Right" Opacity="0.7" VerticalAlignment="Center" Foreground="{DynamicResource ThemeForeground}"/>
                                </Grid>
                            </Button>

                            <Button x:Name="BtnFavorite" Style="{StaticResource SidebarItemStyle}" Tag="Favorite" Click="BtnFilter_Click">
                                <Grid Width="140">
                                    <StackPanel Orientation="Horizontal">
                                        <Path Data="M12,17.27L18.18,21L16.54,13.97L22,9.24L14.81,8.62L12,2L9.19,8.62L2,9.24L7.45,13.97L5.82,21L12,17.27Z" 
                                              Fill="#FFD700" Width="18" Height="18" Stretch="Uniform" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                        <TextBlock x:Name="TxtFavoriteFilter" Text="즐겨찾기" Foreground="{DynamicResource ThemeForeground}" VerticalAlignment="Center"/>
                                    </StackPanel>
                                    <TextBlock x:Name="TxtCountFavorite" Text="0" HorizontalAlignment="Right" Opacity="0.7" VerticalAlignment="Center" Foreground="{DynamicResource ThemeForeground}"/>
                                </Grid>
                            </Button>
                            <Button x:Name="BtnTrash" Style="{StaticResource SidebarItemStyle}" Tag="Trash" Click="BtnFilter_Click">
                                <Grid Width="140">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="🗑️" Margin="0,0,12,0" Opacity="0.9" Foreground="{DynamicResource ThemeForeground}"/>
                                        <TextBlock x:Name="TxtTrashFilter" Text="휴지통" Foreground="{DynamicResource ThemeForeground}"/>
                                    </StackPanel>
                                    <TextBlock x:Name="TxtCountTrash" Text="0" HorizontalAlignment="Right" Opacity="0.7" VerticalAlignment="Center" Foreground="{DynamicResource ThemeForeground}"/>
                                </Grid>
                            </Button>

                            <Button Style="{StaticResource SidebarItemStyle}" Click="BtnSettings_Click" Margin="0,20,0,0">
                                <Grid Width="140">
                                    <StackPanel Orientation="Horizontal">
                                        <Image Source="/icons/setting.png" Width="16" Height="16" Margin="0,0,12,0" VerticalAlignment="Center" Opacity="0.9" RenderOptions.BitmapScalingMode="HighQuality"/>
                                        <TextBlock x:Name="TxtSettingsFilter" Text="히스토리 설정" Foreground="{DynamicResource ThemeForeground}" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Grid>
                            </Button>
                        </StackPanel>
                    </ScrollViewer>
                </Border>

                <!-- 중앙 리스트 영역 (2nd Column) -->
                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/> <!-- Row 1: Action Buttons -->
                        <RowDefinition Height="*"/>    <!-- Row 2: Grid List -->
                        <RowDefinition Height="Auto"/> <!-- Row 3: Pagination -->
                    </Grid.RowDefinitions>

                    <!-- Row 1: Batch Actions -->
                    <Border Grid.Row="0" BorderThickness="0,1,0,1" BorderBrush="{DynamicResource ThemeBorder}" Padding="15,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Orientation="Horizontal">
                                <Button x:Name="BtnSelectAll" Click="BtnSelectAll_Click" Content="전체 선택" Padding="12,5" HorizontalContentAlignment="Center" VerticalAlignment="Center" Cursor="Hand" Background="Transparent" Foreground="{DynamicResource ThemeForeground}" BorderBrush="{DynamicResource ThemeBorder}" BorderThickness="1">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="6"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                                <Button x:Name="BtnDeleteSelected" Click="BtnDeleteSelected_Click" Content="선택 삭제" Margin="10,0,0,0" Padding="12,5" Foreground="#D93025" Background="Transparent" BorderBrush="#D93025" BorderThickness="1">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="6"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                                <Button x:Name="BtnEmptyTrash" Click="BtnEmptyTrash_Click" Content="휴지통 비우기" Margin="10,0,0,0" Padding="12,5" Foreground="#D93025" Background="Transparent" BorderBrush="#D93025" BorderThickness="1" Visibility="Collapsed">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="6"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>

                                <!-- View Mode Selection -->
                                <Border Background="{DynamicResource ThemeSidebarButtonBackground}" BorderBrush="{DynamicResource ThemeBorder}" BorderThickness="1" CornerRadius="4" Margin="15,0,0,0" Padding="1" Height="28" VerticalAlignment="Center">
                                    <StackPanel Orientation="Horizontal">
                                        <Button x:Name="BtnViewList" Click="BtnViewMode_Click" Tag="List" Width="26" Height="26" Style="{StaticResource TextButtonStyle}" Padding="0" Margin="0" ToolTip="리스트형">
                                            <Viewbox Width="14" Height="14">
                                                <Path Data="M3,5H21V7H3V5M3,11H21V13H3V11M3,17H21V19H3V17Z">
                                                    <Path.Style>
                                                        <Style TargetType="Path">
                                                            <Setter Property="Fill" Value="{DynamicResource ThemeForeground}"/>
                                                            <Setter Property="Opacity" Value="0.4"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding CurrentViewMode}" Value="0">
                                                                    <Setter Property="Opacity" Value="1"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Path.Style>
                                                </Path>
                                            </Viewbox>
                                        </Button>
                                        <Button x:Name="BtnViewCard" Click="BtnViewMode_Click" Tag="Card" Width="26" Height="26" Style="{StaticResource TextButtonStyle}" Padding="0" Margin="0" ToolTip="카드형">
                                            <Viewbox Width="14" Height="14">
                                                <Path Data="M3,3H11V11H3V3M13,3H21V11H13V3M3,13H11V21H3V13M13,13H21V21H13V13Z">
                                                    <Path.Style>
                                                        <Style TargetType="Path">
                                                            <Setter Property="Fill" Value="{DynamicResource ThemeForeground}"/>
                                                            <Setter Property="Opacity" Value="0.4"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding CurrentViewMode}" Value="1">
                                                                    <Setter Property="Opacity" Value="1"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Path.Style>
                                                </Path>
                                            </Viewbox>
                                        </Button>
                                    </StackPanel>
                                </Border>
                            </StackPanel>

                            <!-- Right corner Status Info -->
                            <TextBlock x:Name="TxtStatusInfo" Grid.Column="1" Text="" VerticalAlignment="Center" Foreground="{DynamicResource ThemeForeground}" Opacity="0.6" FontSize="12" Margin="0,0,5,0"/>
                        </Grid>
                    </Border>

                    <!-- Row 2: History Grid List -->
                    <ListView Grid.Row="1" x:Name="LstHistory" SelectionMode="Single" SelectionChanged="LstHistory_SelectionChanged" KeyDown="LstHistory_KeyDown" MouseDoubleClick="LstHistory_MouseDoubleClick"
                              BorderThickness="0" Background="Transparent" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Foreground" Value="{DynamicResource ThemeForeground}"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListViewItem">
                                            <Border x:Name="Bd" Background="{TemplateBinding Background}" Padding="4,2">
                                                <Grid>
                                                    <Border x:Name="BottomLine" BorderThickness="0,0,0,1" BorderBrush="{DynamicResource ThemeBorder}" Opacity="0.4"/>
                                                    <GridViewRowPresenter x:Name="PART_GridViewRowPresenter" Content="{TemplateBinding Content}" Columns="{TemplateBinding GridView.ColumnCollection}"/>
                                                    <ContentPresenter x:Name="PART_ContentPresenter" Visibility="Collapsed"/>
                                                </Grid>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ThemeSidebarButtonHoverBackground}"/>
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ThemeSidebarButtonHoverBackground}"/>
                                                </Trigger>
                                                <!-- Handle List vs Card templates switching -->
                                                <DataTrigger Binding="{Binding View, ElementName=LstHistory}" Value="{x:Null}">
                                                    <Setter TargetName="PART_GridViewRowPresenter" Property="Visibility" Value="Collapsed"/>
                                                    <Setter TargetName="BottomLine" Property="Visibility" Value="Collapsed"/>
                                                    <Setter TargetName="PART_ContentPresenter" Property="Visibility" Value="Visible"/>
                                                    <Setter Property="GridView.ColumnCollection" Value="{x:Null}"/>
                                                    <!-- In Card mode, don't show the default selection rectangle around our rounded card -->
                                                    <Setter TargetName="Bd" Property="Background" Value="Transparent"/>
                                                    <Setter TargetName="Bd" Property="Padding" Value="0"/>
                                                    <Setter Property="Background" Value="Transparent"/>
                                                </DataTrigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.View>
                            <GridView x:Name="HistoryGridView">
                                <GridViewColumn x:Name="ColCheckBox" Width="45">
                                    <GridViewColumn.Header>
                                        <CheckBox x:Name="ChkHeaderAll" Click="BtnSelectAll_Click" HorizontalAlignment="Center"/>
                                    </GridViewColumn.Header>
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <CheckBox IsChecked="{Binding IsSelected}" VerticalAlignment="Center" HorizontalAlignment="Center" Click="ChkItem_Click"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn x:Name="ColDate" Header="날짜 및 시각" Width="132">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding FormattedDate}" Foreground="{DynamicResource ThemeForeground}" VerticalAlignment="Center"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn x:Name="ColFileName" Header="파일명" Width="205">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding FileName}" Foreground="{DynamicResource ThemeForeground}" TextTrimming="CharacterEllipsis" MaxWidth="195" VerticalAlignment="Center">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsPinned}" Value="True">
                                                                <Setter Property="FontWeight" Value="Bold"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn x:Name="ColMeta" Header="출처/제목" Width="180">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding MetaInfo}" Foreground="{DynamicResource ThemeForeground}" Opacity="0.7" TextTrimming="CharacterEllipsis" VerticalAlignment="Center">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsPinned}" Value="True">
                                                                <Setter Property="FontWeight" Value="Bold"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn x:Name="ColMemo" Header="메모" Width="205">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Memo, Converter={StaticResource FirstLineConverter}}" Foreground="{DynamicResource ThemeForeground}" Opacity="0.9" TextTrimming="CharacterEllipsis" VerticalAlignment="Center">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsPinned}" Value="True">
                                                                <Setter Property="FontWeight" Value="Bold"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn x:Name="ColPin" Width="53">
                                    <GridViewColumn.Header>
                                        <Path Data="M16,12V4H17V2H7V4H8V12L6,14V16H11.2V22H12.8V16H18V14L16,12Z" 
                                              Fill="{DynamicResource ThemeForeground}" Width="14" Height="14" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.5"/>
                                    </GridViewColumn.Header>
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Click="BtnPin_Click" Background="Transparent" BorderThickness="0" Cursor="Hand" HorizontalAlignment="Center">
                                                <Grid>
                                                    <Path x:Name="PinFilled" Data="M16,12V4H17V2H7V4H8V12L6,14V16H11.2V22H12.8V16H18V14L16,12Z" 
                                                          Fill="#FF5252" Width="14" Height="14" Stretch="Uniform" Visibility="Collapsed"/>
                                                    <Path x:Name="PinOutline" Data="M16,12V4H17V2H7V4H8V12L6,14V16H11.2V22H12.8V16H18V14L16,12M14.2,14H9.8L10,12.2V4H14V12.2L14.2,14Z" 
                                                          Fill="{DynamicResource ThemeForeground}" Opacity="0.3" Width="14" Height="14" Stretch="Uniform"/>
                                                </Grid>
                                            </Button>
                                            <DataTemplate.Triggers>
                                                <DataTrigger Binding="{Binding IsPinned}" Value="True">
                                                    <Setter TargetName="PinFilled" Property="Visibility" Value="Visible"/>
                                                    <Setter TargetName="PinOutline" Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </DataTemplate.Triggers>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn x:Name="ColFavorite" Width="53">
                                    <GridViewColumn.Header>
                                        <Path Data="M12,17.27L18.18,21L16.54,13.97L22,9.24L14.81,8.62L12,2L9.19,8.62L2,9.24L7.45,13.97L5.82,21L12,17.27Z" 
                                              Fill="{DynamicResource ThemeForeground}" Width="14" Height="14" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </GridViewColumn.Header>
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Click="BtnFavorite_Click" Background="Transparent" BorderThickness="0" Cursor="Hand" HorizontalAlignment="Center">
                                                <Grid>
                                                    <Path x:Name="StarFilled" Data="M12,17.27L18.18,21L16.54,13.97L22,9.24L14.81,8.62L12,2L9.19,8.62L2,9.24L7.45,13.97L5.82,21L12,17.27Z" 
                                                          Fill="#FFD700" Width="15" Height="15" Stretch="Uniform" Visibility="Collapsed"/>
                                                    <Path x:Name="StarOutline" Data="M12,15.39L8.24,17.66L9.23,13.38L5.91,10.5L10.29,10.13L12,6.09L13.71,10.13L18.09,10.5L14.77,13.38L15.76,17.66L12,15.39M22,9.24L14.81,8.62L12,2L9.19,8.62L2,9.24L7.45,13.97L5.82,21L12,17.27L18.18,21L16.54,13.97L22,9.24Z" 
                                                          Fill="{DynamicResource ThemeForeground}" Opacity="0.3" Width="15" Height="15" Stretch="Uniform"/>
                                                </Grid>
                                            </Button>
                                            <DataTemplate.Triggers>
                                                <DataTrigger Binding="{Binding IsFavorite}" Value="True">
                                                    <Setter TargetName="StarFilled" Property="Visibility" Value="Visible"/>
                                                    <Setter TargetName="StarOutline" Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </DataTemplate.Triggers>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn x:Name="ColActions" Header="작업" Width="53">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Click="BtnRestore_Click" Visibility="{Binding IsInTrash, Converter={StaticResource BoolToVis}}" Background="Transparent" BorderThickness="0" Cursor="Hand" Padding="4" ToolTip="복원">
                                                    <Path Data="M12,5V1L7,6L12,11V7A6,6 0 0,1 18,13A6,6 0 0,1 12,19A6,6 0 0,1 6,13H4A8,8 0 0,0 12,21A8,8 0 0,0 20,13A8,8 0 0,0 12,5Z" 
                                                          Fill="{DynamicResource ThemeForeground}" Opacity="0.8" Width="14" Height="14" Stretch="Uniform"/>
                                                </Button>
                                                <Button Click="BtnDeleteItem_Click" Background="Transparent" BorderThickness="0" Cursor="Hand" Padding="4" ToolTip="삭제">
                                                    <Path Data="M9,3V4H4V6H5V19A2,2 0 0,0 7,21H17A2,2 0 0,0 19,19V6H20V4H15V3H9M7,6H17V19H7V6M9,8V17H11V8H9M13,8V17H15V8H13Z" 
                                                          Fill="{DynamicResource ThemeForeground}" Opacity="0.4" Width="14" Height="14" Stretch="Uniform"/>
                                                </Button>
                                            </StackPanel>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>

                    <!-- Row 3: Pagination -->
                    <Border Grid.Row="2" BorderThickness="0,1,0,0" BorderBrush="{DynamicResource ThemeBorder}" Padding="15,10">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <Button x:Name="BtnFirstPage" Click="BtnFirstPage_Click" Style="{StaticResource PaginationButtonStyle}" Content="&lt;&lt;" Width="30" Height="30" Margin="2,0"/>
                            <Button x:Name="BtnPrevPage" Click="BtnPrevPage_Click" Style="{StaticResource PaginationButtonStyle}" Content="&lt;" Width="30" Height="30" Margin="2,0"/>
                            
                            <StackPanel x:Name="PanelPageNumbers" Orientation="Horizontal" Margin="5,0"/>
                            
                            <Button x:Name="BtnNextPage" Click="BtnNextPage_Click" Style="{StaticResource PaginationButtonStyle}" Content="&gt;" Width="30" Height="30" Margin="2,0"/>
                            <Button x:Name="BtnLastPage" Click="BtnLastPage_Click" Style="{StaticResource PaginationButtonStyle}" Content="&gt;&gt;" Width="30" Height="30" Margin="2,0"/>
                            
                            <TextBlock x:Name="TxtPageInfo" Text="1 / 1" VerticalAlignment="Center" Margin="15,0,0,0" Foreground="{DynamicResource ThemeForeground}" Opacity="0.6"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Column Splitter -->
                <GridSplitter Grid.Column="2" Width="4" HorizontalAlignment="Stretch" Background="{DynamicResource ThemeBorder}" Cursor="SizeWE"/>

                <!-- Preview Pane (3rd Column: Image Top, Details Bottom) -->
                <Border Grid.Column="3" Background="{DynamicResource ThemeBackground}" BorderThickness="1,0,0,0" BorderBrush="{DynamicResource ThemeBorder}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>   <!-- Image -->
                            <RowDefinition Height="Auto"/> <!-- Simple Memo -->
                            <RowDefinition Height="Auto"/> <!-- Details -->
                        </Grid.RowDefinitions>

                        <!-- Image Display -->
                        <Border Grid.Row="0" Margin="15" Background="#08000000" CornerRadius="8">
                            <Grid x:Name="BtnImgPreviewGrid" Background="Transparent" Cursor="Hand" MouseLeftButtonDown="ImgPreview_MouseDown" ToolTip="윈도우 기본 뷰어로 열기">
                                 <Image x:Name="ImgPreview" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality" IsHitTestVisible="False"/>
                                
                                <!-- [추가] 미디어용 오버레이 (비디오 플레이 버튼, 오디오 아이콘, 포맷 뱃지) -->
                                <Grid x:Name="MediaOverlay" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Visibility="Collapsed">
                                    <!-- Play Button -->
                                    <Grid x:Name="VideoPlayOverlay">
                                        <Ellipse Width="60" Height="60" Fill="#B4000000" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        <TextBlock Text="▶" FontSize="28" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5,0,0,0"/>
                                    </Grid>
                                    <!-- Audio Icon -->
                                    <TextBlock x:Name="AudioIconOverlay" Text="🔊" FontSize="80" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"/>
                                    
                                    <!-- Format Badge -->
                                    <Border HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,15,15,0" 
                                            Background="#C84361EE" CornerRadius="4" Padding="8,4">
                                        <TextBlock x:Name="TxtPreviewFormat" Text="MP4" FontSize="18" FontWeight="Bold" Foreground="White"/>
                                    </Border>
                                </Grid>

                                <!-- Interactive Buttons Overlay -->
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,10,10">
                                    <Button x:Name="BtnPreviewEdit" Click="BtnPreviewEdit_Click" Width="36" Height="36" Margin="0,0,8,0" Background="#CC2D2D30" BorderThickness="0" Cursor="Hand" ToolTip="이미지 편집">
                                        <Button.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="18"/>
                                            </Style>
                                        </Button.Resources>
                                        <Image Source="icons/img_editor.png" Width="18" Height="18" Stretch="Uniform"/>
                                    </Button>
                                    <Button x:Name="BtnPreviewNote" Click="BtnPreviewNote_Click" Width="36" Height="36" Margin="0,0,8,0" Background="#CC2D2D30" BorderThickness="0" Cursor="Hand" ToolTip="내 노트로 저장">
                                        <Button.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="18"/>
                                            </Style>
                                        </Button.Resources>
                                        <Image Source="icons/my_note.png" Width="18" Height="18" Stretch="Uniform"/>
                                    </Button>
                                    <Button x:Name="BtnPreviewPin" Click="BtnPreviewPin_Click" Width="36" Height="36" Background="#CC2D2D30" BorderThickness="0" Cursor="Hand" ToolTip="화면에 고정">
                                        <Button.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="18"/>
                                            </Style>
                                        </Button.Resources>
                                        <Image Source="icons/pin.png" Width="18" Height="18" Stretch="Uniform"/>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- Simple Memo (Above Details) -->
                        <Border Grid.Row="1" Padding="20,15,20,5">
                            <StackPanel>
                                <Grid Margin="0,0,0,10">
                                    <TextBlock x:Name="TxtSimpleMemoLabel" Text="간단 메모" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource ThemeForeground}" VerticalAlignment="Center"/>
                                    <Button x:Name="BtnEditMemo" Click="BtnEditMemo_Click" Content="수정" HorizontalAlignment="Right" Background="Transparent" BorderBrush="{DynamicResource ThemeBorder}" BorderThickness="1" Padding="8,2" Foreground="{DynamicResource ThemeForeground}" Cursor="Hand">
                                        <Button.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="4"/>
                                            </Style>
                                        </Button.Resources>
                                    </Button>
                                </Grid>
                                
                                <Grid x:Name="MemoEditArea">
                                    <ScrollViewer x:Name="ScrollMemoDisplay" MaxHeight="150" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="0,0,0,10">
                                        <TextBlock x:Name="TxtMemoDisplay" Text="메모가 없습니다." TextWrapping="Wrap" Foreground="{DynamicResource ThemeForeground}" Opacity="0.7" MinHeight="40"/>
                                    </ScrollViewer>
                                    <TextBox x:Name="EditMemoBox" Visibility="Collapsed" TextWrapping="Wrap" AcceptsReturn="True" MinHeight="60" MaxHeight="120" VerticalScrollBarVisibility="Auto" Margin="0,0,0,10" Background="{DynamicResource ThemeSidebarButtonBackground}" BorderBrush="{DynamicResource ThemeBorder}" Foreground="{DynamicResource ThemeForeground}"/>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <!-- Details (Bottom) -->
                        <Border Grid.Row="2" Padding="20" BorderThickness="0,1,0,0" BorderBrush="{DynamicResource ThemeBorder}">
                            <StackPanel>
                                <TextBlock x:Name="TxtDetailsLabel" Text="상세 정보" FontSize="14" FontWeight="Bold" Margin="0,0,0,15" Foreground="{DynamicResource ThemeForeground}"/>
                                <Grid Margin="0,0,0,8">
                                    <TextBlock x:Name="TxtSizeLabel" Text="크기:" Foreground="{DynamicResource ThemeForeground}" Opacity="0.6"/>
                                    <TextBlock x:Name="TxtPreviewSize" Text="-" HorizontalAlignment="Right" Foreground="{DynamicResource ThemeForeground}"/>
                                </Grid>
                                <Grid Margin="0,0,0,8">
                                    <TextBlock x:Name="TxtCapacityLabel" Text="용량:" Foreground="{DynamicResource ThemeForeground}" Opacity="0.6"/>
                                    <TextBlock x:Name="TxtPreviewWeight" Text="-" HorizontalAlignment="Right" Foreground="{DynamicResource ThemeForeground}"/>
                                </Grid>
                                <Grid Margin="0,0,0,8">
                                    <TextBlock x:Name="TxtAppLabel" Text="앱:" Foreground="{DynamicResource ThemeForeground}" Opacity="0.6"/>
                                    <TextBlock x:Name="TxtPreviewApp" Text="-" HorizontalAlignment="Right" Foreground="{DynamicResource ThemeForeground}"/>
                                </Grid>
                                <StackPanel Margin="0,15,0,0">
                                    <TextBlock x:Name="TxtPathLabel" Text="상세 경로" FontSize="11" Opacity="0.6" Foreground="{DynamicResource ThemeForeground}" Margin="0,0,0,5"/>
                                    <Grid>
                                        <TextBlock x:Name="TxtPreviewPath" Text="-" Foreground="{DynamicResource ThemeForeground}" Opacity="0.8" TextWrapping="Wrap" MaxWidth="250" HorizontalAlignment="Left" TextTrimming="None" Margin="0,0,30,0"/>
                                        <Button x:Name="BtnOpenFolder" Click="BtnOpenFolder_Click" Background="Transparent" BorderThickness="0" Cursor="Hand" Padding="0" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                                            <Path Data="M10,4H4C2.89,4 2,4.89 2,6V18A2,2 0 0,0 4,20H20A2,2 0 0,0 22,18V8C22,6.89 21.1,6 20,6H12L10,4Z" 
                                                  Fill="#FFD600" Width="18" Height="18" Stretch="Uniform"/>
                                        </Button>
                                    </Grid>
                                </StackPanel>
                                <TextBlock x:Name="TxtTitleLabel" Text="전체 제목:" Foreground="{DynamicResource ThemeForeground}" Opacity="0.6" Margin="0,15,0,5"/>
                                <TextBlock x:Name="TxtPreviewTitle" Text="-" TextWrapping="Wrap" FontWeight="SemiBold" Foreground="{DynamicResource ThemeForeground}"/>
                            </StackPanel>
                        </Border>

                        <!-- Nothing Selected Empty State -->
                        <Grid x:Name="PreviewEmptyState" Grid.RowSpan="3" Background="{DynamicResource ThemeBackground}">
                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                <Image Source="/icons/icon_main.png" Width="200" Opacity="0.5" Margin="0,0,0,20"/>
                                <TextBlock x:Name="TxtEmptySelection" Text="상세 내역을 보려면 항목을 선택하세요" Foreground="{DynamicResource ThemeForeground}" Opacity="0.5" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>
            
            <!-- Bottom Tip Bar -->
            <Border Grid.Row="2" Background="{DynamicResource ThemeSidebarButtonBackground}" BorderThickness="0,1,0,0" BorderBrush="{DynamicResource ThemeBorder}" Height="40" Padding="30,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border x:Name="BrdTipLabel" Background="#8E2DE2" CornerRadius="4" Padding="6,2" Margin="0,0,12,0">
                        <TextBlock x:Name="TxtTipLabel" Text="TIP" Foreground="White" FontSize="10" FontWeight="Bold"/>
                    </Border>
                    <TextBlock x:Name="TxtRollingTip" Text="비밀번호 설정으로 내 노트를 안전하게 보호하세요." 
                               Foreground="{DynamicResource ThemeForeground}" Opacity="0.6" FontSize="12" VerticalAlignment="Center">
                        <TextBlock.Triggers>
                            <EventTrigger RoutedEvent="TextBlock.Loaded">
                                <BeginStoryboard>
                                    <Storyboard x:Name="TipFadeAnimation">
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </TextBlock.Triggers>
                    </TextBlock>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
