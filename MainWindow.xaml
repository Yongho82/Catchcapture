<Window x:Class="CatchCapture.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CatchCapture"
        mc:Ignorable="d"
        Title="캣치" Height="500" Width="350" 
        MinWidth="350" MinHeight="500"
        WindowStartupLocation="CenterScreen"
        Background="#F5F7FA">
    
    <Window.Resources>
        <!-- 버튼 스타일 -->
        <Style x:Key="SidebarButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Margin" Value="5,3,5,3"/>
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="0">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#D0D0D0" TargetName="border"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#C0C0C0" TargetName="border"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5" TargetName="border"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 구분선 스타일 -->
        <Style x:Key="SeparatorStyle" TargetType="Separator">
            <Setter Property="Margin" Value="5,3,5,3"/>
            <Setter Property="Background" Value="#CCCCCC"/>
            <Setter Property="Height" Value="1"/>
        </Style>
        
        <!-- 스크롤바 스타일 -->
        <Style x:Key="SimpleScrollBar" TargetType="{x:Type ScrollBar}">
            <Setter Property="Width" Value="8"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid>
                            <Border Background="#F0F0F0" CornerRadius="4"/>
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate>
                                                <Border Background="#BBBBBB" CornerRadius="4"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- 왼쪽 버튼 영역 -->
        <Border Grid.Column="0" Background="#F0F0F0" BorderThickness="0,0,1,0" BorderBrush="#CCCCCC">
            <StackPanel Margin="0,10,0,10">
                <Button x:Name="AreaCaptureButton" Style="{StaticResource SidebarButtonStyle}" Click="AreaCaptureButton_Click">
                    <TextBlock Text="영역 캡처" HorizontalAlignment="Center" FontSize="11"/>
                </Button>
                
                <Button x:Name="FullScreenCaptureButton" Style="{StaticResource SidebarButtonStyle}" Click="FullScreenCaptureButton_Click">
                    <TextBlock Text="전체화면" HorizontalAlignment="Center" FontSize="11"/>
                </Button>
                
                <Separator Style="{StaticResource SeparatorStyle}"/>
                
                <Button x:Name="CopySelectedButton" Style="{StaticResource SidebarButtonStyle}" Click="CopySelectedButton_Click" IsEnabled="False">
                    <TextBlock Text="복사" HorizontalAlignment="Center" FontSize="11"/>
                </Button>
                
                <Button x:Name="CopyAllButton" Style="{StaticResource SidebarButtonStyle}" Click="CopyAllButton_Click" IsEnabled="False">
                    <TextBlock Text="전체복사" HorizontalAlignment="Center" FontSize="11"/>
                </Button>
                
                <Separator Style="{StaticResource SeparatorStyle}"/>
                
                <Button x:Name="SaveSelectedButton" Style="{StaticResource SidebarButtonStyle}" Click="SaveSelectedButton_Click" IsEnabled="False">
                    <TextBlock Text="저장" HorizontalAlignment="Center" FontSize="11"/>
                </Button>
                
                <Button x:Name="SaveAllButton" Style="{StaticResource SidebarButtonStyle}" Click="SaveAllButton_Click" IsEnabled="False">
                    <TextBlock Text="전체저장" HorizontalAlignment="Center" FontSize="11"/>
                </Button>
                
                <Separator Style="{StaticResource SeparatorStyle}"/>
                
                <Button x:Name="DeleteSelectedButton" Style="{StaticResource SidebarButtonStyle}" Click="DeleteSelectedButton_Click" IsEnabled="False">
                    <TextBlock Text="삭제" HorizontalAlignment="Center" FontSize="11"/>
                </Button>
                
                <Button x:Name="DeleteAllButton" Style="{StaticResource SidebarButtonStyle}" Click="DeleteAllButton_Click" IsEnabled="False">
                    <TextBlock Text="전체삭제" HorizontalAlignment="Center" FontSize="11"/>
                </Button>
                
                <Separator Style="{StaticResource SeparatorStyle}"/>
                
                <Button x:Name="SimpleModeButton" Style="{StaticResource SidebarButtonStyle}" Click="SimpleModeButton_Click">
                    <TextBlock Text="간편 모드" HorizontalAlignment="Center" FontSize="11"/>
                </Button>
            </StackPanel>
        </Border>

        <!-- 오른쪽 캡처 표시 영역 -->
        <Border Grid.Column="1" Background="#FFFFFF" Margin="0">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <ScrollViewer.Resources>
                    <Style TargetType="{x:Type ScrollBar}" BasedOn="{StaticResource SimpleScrollBar}"/>
                </ScrollViewer.Resources>
                <!-- 단일 세로 열로 표시 -->
                <StackPanel x:Name="CaptureListPanel" Margin="10"/>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>
