<Window x:Class="CatchCapture.DraftListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="임시저장 목록" Height="450" Width="400"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False">

    <Border Background="{DynamicResource ThemeBackground}" CornerRadius="15" BorderThickness="1" BorderBrush="{DynamicResource ThemeWindowBorder}">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" Color="Black" Opacity="0.2" ShadowDepth="0"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="{DynamicResource ThemeTitleBackground}" CornerRadius="15,15,0,0">
                <Grid Margin="20,0">
                    <TextBlock Text="임시저장 목록" VerticalAlignment="Center" FontWeight="Bold" FontSize="15" Foreground="{DynamicResource ThemeTitleForeground}"/>
                    <Button HorizontalAlignment="Right" Click="BtnClose_Click" Background="Transparent" BorderThickness="0" Cursor="Hand">
                        <Path Data="M19,6.41L17.59,5L12,10.59L6.41,5L5,6.41L10.59,12L5,17.59L6.41,19L12,13.41L17.59,19L19,17.59L13.41,12L19,6.41Z" 
                              Fill="{DynamicResource ThemeTitleForeground}" Stretch="Uniform" Width="12" Height="12"/>
                    </Button>
                </Grid>
            </Border>

            <!-- List -->
            <ListBox x:Name="ListDrafts" Grid.Row="1" Background="Transparent" BorderThickness="0" Margin="10"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled" d:ItemsSource="{d:SampleData ItemCount=5}">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0,0,0,8"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="8" BorderThickness="1" BorderBrush="Transparent" x:Name="ItemBorder">
                                        <ContentPresenter />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource ThemeHoverBackground}"/>
                                            <Setter TargetName="ItemBorder" Property="BorderBrush" Value="{DynamicResource ThemeBorder}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="12,10">
                            <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{Binding DisplayTitle}" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource ThemeForeground}" TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text="{Binding DisplayTime}" FontSize="11" Foreground="{DynamicResource ThemeForeground}" Opacity="0.5" Margin="0,2,0,0"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                <Button Content="복구" Click="BtnRestore_Click" Cursor="Hand" Padding="12,5" Background="#3498db" Foreground="White" BorderThickness="0" FontWeight="Bold">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="6"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                                <Button Click="BtnDelete_Click" Margin="5,0,0,0" Cursor="Hand" Background="Transparent" BorderThickness="0" ToolTip="삭제">
                                    <Path Data="M19,4H15.5L14.5,3H9.5L8.5,4H5V6H19V4M6,19A2,2 0 0,0 8,21H16A2,2 0 0,0 18,19V7H6V19Z" Fill="#FF5C5C" Stretch="Uniform" Width="12" Height="12"/>
                                </Button>
                            </StackPanel>
                        </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- Footer -->
            <Border Grid.Row="2" Padding="15,10" BorderThickness="0,1,0,0" BorderBrush="{DynamicResource ThemeWindowBorder}">
                <Button Content="임시저장 전체 삭제" Click="BtnClearAll_Click" Foreground="#FF5C5C" Background="Transparent" BorderThickness="0" Cursor="Hand" HorizontalAlignment="Right" FontSize="12"/>
            </Border>
        </Grid>
    </Border>
</Window>
