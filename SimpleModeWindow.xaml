<Window x:Class="CatchCapture.SimpleModeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CatchCapture"
        mc:Ignorable="d"
        Title="간편 모드" Height="85" Width="200"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ResizeMode="NoResize" ShowInTaskbar="False"
        MouseLeftButtonDown="Window_MouseLeftButtonDown" MouseEnter="Window_MouseEnter" MouseLeave="Window_MouseLeave"
        KeyDown="SimpleModeWindow_KeyDown">
    
    <Window.Resources>
        <!-- 공통 버튼 스타일 -->
        <Style x:Key="SimpleModeButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#EEF2FF" TargetName="border"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 아이콘 버튼 스타일 -->
        <Style x:Key="IconButtonStyle" TargetType="Button" BasedOn="{StaticResource SimpleModeButtonStyle}">
            <Setter Property="Width" Value="36"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Margin" Value="2"/>
        </Style>

        <!-- 작은 제어 버튼 스타일 -->
        <Style x:Key="ControlButtonStyle" TargetType="Button" BasedOn="{StaticResource SimpleModeButtonStyle}">
            <Setter Property="Width" Value="20"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Margin" Value="1"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- 가로 모드 레이아웃 -->
        <Border x:Name="HorizontalRoot" CornerRadius="8" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" Visibility="Visible">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="24"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- 타이틀바 -->
                <Border Grid.Row="0" Background="#F8F9FA" CornerRadius="8,8,0,0" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
                    <Grid>
                        <TextBlock Text="≡" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#999" FontWeight="Bold"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,5,0">
                            <Button Style="{StaticResource ControlButtonStyle}" Click="MinimizeToTrayButton_Click" ToolTip="트레이 모드">
                                <Image Source="/icons/tray_mode.png" Width="12" Height="12"/>
                            </Button>
                            <Button Style="{StaticResource ControlButtonStyle}" Click="ExitSimpleModeButton_Click" ToolTip="일반 모드">
                                <Image Source="/icons/window_cap.png" Width="12" Height="12"/>
                            </Button>
                            <Button Style="{StaticResource ControlButtonStyle}" Click="CloseButton_Click" ToolTip="닫기">
                                <TextBlock Text="✕" FontSize="9" Foreground="#666"/>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>
                
                <!-- 버튼 영역 -->
                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Button Style="{StaticResource IconButtonStyle}" Click="AreaCaptureButton_Click" ToolTip="영역 캡처">
                        <Image Source="/icons/area_capture.png" Width="20" Height="20"/>
                    </Button>
                    <Button x:Name="DelayBtnH" Style="{StaticResource IconButtonStyle}" Click="DelayIconButton_Click" ToolTip="지연 캡처">
                        <Image Source="/icons/clock.png" Width="20" Height="20"/>
                        <Button.ContextMenu>
                            <ContextMenu x:Name="DelayMenuH">
                                <MenuItem Header="3초" Tag="3" Click="DelayOption_Click"/>
                                <MenuItem Header="5초" Tag="5" Click="DelayOption_Click"/>
                                <MenuItem Header="10초" Tag="10" Click="DelayOption_Click"/>
                            </ContextMenu>
                        </Button.ContextMenu>
                    </Button>
                    <Button Style="{StaticResource IconButtonStyle}" Click="FullScreenCaptureButton_Click" ToolTip="전체화면">
                        <Image Source="/icons/full_screen.png" Width="20" Height="20"/>
                    </Button>
                    <Button Style="{StaticResource IconButtonStyle}" Click="DesignatedButton_Click" ToolTip="지정캡처">
                        <Image Source="/icons/designated.png" Width="20" Height="20"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 세로 모드 레이아웃 -->
        <Border x:Name="VerticalRoot" CornerRadius="8" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" Visibility="Collapsed">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="24"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- 드래그 핸들 -->
                <Border Grid.Row="0" Background="#F8F9FA" CornerRadius="8,8,0,0" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
                    <TextBlock Text="≡" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#999" FontWeight="Bold"/>
                </Border>

                <!-- 캡처 버튼들 -->
                <StackPanel Grid.Row="1" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,5">
                    <Button Style="{StaticResource IconButtonStyle}" Click="AreaCaptureButton_Click" ToolTip="영역 캡처">
                        <Image Source="/icons/area_capture.png" Width="20" Height="20"/>
                    </Button>
                    <Button x:Name="DelayBtnV" Style="{StaticResource IconButtonStyle}" Click="DelayIconButton_Click" ToolTip="지연 캡처">
                        <Image Source="/icons/clock.png" Width="20" Height="20"/>
                        <Button.ContextMenu>
                            <ContextMenu x:Name="DelayMenuV">
                                <MenuItem Header="3초" Tag="3" Click="DelayOption_Click"/>
                                <MenuItem Header="5초" Tag="5" Click="DelayOption_Click"/>
                                <MenuItem Header="10초" Tag="10" Click="DelayOption_Click"/>
                            </ContextMenu>
                        </Button.ContextMenu>
                    </Button>
                    <Button Style="{StaticResource IconButtonStyle}" Click="FullScreenCaptureButton_Click" ToolTip="전체화면">
                        <Image Source="/icons/full_screen.png" Width="20" Height="20"/>
                    </Button>
                    <Button Style="{StaticResource IconButtonStyle}" Click="DesignatedButton_Click" ToolTip="지정캡처">
                        <Image Source="/icons/designated.png" Width="20" Height="20"/>
                    </Button>
                </StackPanel>

                <!-- 하단 제어 버튼들 (닫기만 남김) -->
                <Border Grid.Row="2" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0" Padding="0,5">
                    <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                        <Button Style="{StaticResource ControlButtonStyle}" Click="CloseButton_Click" ToolTip="닫기">
                            <TextBlock Text="✕" FontSize="9" Foreground="#666"/>
                        </Button>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>