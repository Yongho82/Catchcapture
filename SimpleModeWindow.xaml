<Window x:Class="CatchCapture.SimpleModeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CatchCapture"
        mc:Ignorable="d"
        Title="간편 모드" Height="75" Width="230"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ResizeMode="NoResize" ShowInTaskbar="False"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    
    <Window.Resources>
        <!-- 버튼 스타일 -->
        <Style x:Key="SimpleModeButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="72"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Margin" Value="1"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="1" BlurRadius="3" Opacity="0.1" Direction="270"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#EEF2FF" TargetName="border"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#D8E0FF" TargetName="border"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 닫기/모드 전환 버튼 스타일 -->
        <Style x:Key="SmallButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="20"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#50FFFFFF" TargetName="border"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#30FFFFFF" TargetName="border"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 텍스트 렌더링 개선 스타일 -->
        <Style x:Key="ClearTextStyle" TargetType="TextBlock">
            <Setter Property="TextOptions.TextFormattingMode" Value="Ideal"/>
            <Setter Property="TextOptions.TextRenderingMode" Value="Auto"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="UseLayoutRounding" Value="True"/>
        </Style>
    </Window.Resources>
    
    <Border CornerRadius="8" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="2" BlurRadius="8" Opacity="0.3" Direction="270"/>
        </Border.Effect>
        <Border.BorderBrush>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#4361EE" Offset="0.0"/>
                <GradientStop Color="#3A56D4" Offset="1.0"/>
            </LinearGradientBrush>
        </Border.BorderBrush>
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#4361EE" Offset="0.0"/>
                <GradientStop Color="#3A56D4" Offset="1.0"/>
            </LinearGradientBrush>
        </Border.Background>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="24"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- 제목 영역 -->
            <Border Grid.Row="0" Padding="10,2,10,2" Background="Transparent" ClipToBounds="True">
                <Grid>
                    <StackPanel Orientation="Horizontal">
                        <Image Source="/icons/simple_mode.png" Width="14" Height="14" Margin="0,0,5,0" 
                               RenderOptions.BitmapScalingMode="HighQuality"/>
                        <TextBlock Text="간편 캡처" Foreground="White" FontWeight="SemiBold" FontSize="12"
                                  Style="{StaticResource ClearTextStyle}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="CloseButton" Style="{StaticResource SmallButtonStyle}"
                                Click="CloseButton_Click" ToolTip="닫기">
                            <TextBlock Text="✕" FontSize="10" Foreground="White" Style="{StaticResource ClearTextStyle}"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>
            
            <!-- 버튼 영역 -->
            <Border Grid.Row="1" Padding="4,4,4,6">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Button x:Name="AreaCaptureButton" Style="{StaticResource SimpleModeButtonStyle}" Click="AreaCaptureButton_Click">
                        <StackPanel Orientation="Horizontal">
                            <Image Source="/icons/area_capture.png" Width="16" Height="16" Margin="0,0,3,0"
                                  RenderOptions.BitmapScalingMode="HighQuality"/>
                            <TextBlock Text="영역 캡처" Style="{StaticResource ClearTextStyle}" FontSize="11"/>
                        </StackPanel>
                    </Button>
                    
                    <Button x:Name="FullScreenCaptureButton" Style="{StaticResource SimpleModeButtonStyle}" Click="FullScreenCaptureButton_Click">
                        <StackPanel Orientation="Horizontal">
                            <Image Source="/icons/full_screen.png" Width="16" Height="16" Margin="0,0,3,0"
                                  RenderOptions.BitmapScalingMode="HighQuality"/>
                            <TextBlock Text="전체화면" Style="{StaticResource ClearTextStyle}" FontSize="11"/>
                        </StackPanel>
                    </Button>
                    
                    <Button x:Name="ExitSimpleModeButton" Style="{StaticResource SimpleModeButtonStyle}" Click="ExitSimpleModeButton_Click">
                        <StackPanel Orientation="Horizontal">
                            <Image Source="/icons/simple_mode.png" Width="16" Height="16" Margin="0,0,3,0"
                                  RenderOptions.BitmapScalingMode="HighQuality"/>
                            <TextBlock Text="일반 모드" Style="{StaticResource ClearTextStyle}" FontSize="11"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window> 