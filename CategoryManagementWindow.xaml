<Window x:Class="CatchCapture.CategoryManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="분류 관리" Height="450" Width="400"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    
    <Border Background="White" CornerRadius="15" BorderThickness="1" BorderBrush="#E0E0E0">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" Color="Black" Opacity="0.1" ShadowDepth="5" Direction="270"/>
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Header (Title Bar) -->
            <Border Grid.Row="0" Background="#F3E5F5" CornerRadius="14,14,0,0" Padding="20">
                <StackPanel>
                    <TextBlock x:Name="TxtTitle" Text="분류 관리" FontSize="20" FontWeight="Bold" Foreground="#333333"/>
                    <TextBlock x:Name="TxtDesc" Text="메모를 분류할 그룹을 관리하세요." FontSize="13" Foreground="#777777" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
            
            <!-- Content -->
            <Grid Grid.Row="1" Margin="20,10,20,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- List -->
                <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="CategoriesList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#F8F9FA" CornerRadius="8" Margin="0,0,0,8" Padding="12,8">
                                    <Grid>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <Ellipse Width="10" Height="10" Fill="{Binding Color}" Margin="0,0,10,0"/>
                                            <TextBlock Text="{Binding Name}" FontSize="15" Foreground="#333333" FontWeight="Medium"/>
                                        </StackPanel>
                                        <Button HorizontalAlignment="Right" Background="Transparent" BorderThickness="0" 
                                                Tag="{Binding Id}" Click="BtnDeleteCategory_Click" Cursor="Hand">
                                            <Path Data="M19,6.41L17.59,5L12,10.59L6.41,5L5,6.41L10.59,12L5,17.59L6.41,19L12,13.41L17.59,19L19,17.59L13.41,12L19,6.41Z" 
                                                  Fill="#FF5252" Stretch="Uniform" Width="12" Height="12"/>
                                        </Button>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
                
                <!-- Input Area -->
                <Border Grid.Row="1" Background="#F1F3F5" CornerRadius="8" Padding="15" Margin="0,15,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="TxtNewCategory" Background="Transparent" BorderThickness="0" FontSize="14" 
                                 VerticalAlignment="Center" Padding="5" Tag="새 분류 입력..."/>
                        <Button Grid.Column="1" x:Name="BtnAddCategory" Click="BtnAddCategory_Click" Width="32" Height="32" 
                                Background="#8E2DE2" Foreground="White" BorderThickness="0" Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="16">
                                        <Path Data="M19,13H13V19H11V13H5V11H11V5H13V11H19V13Z" Fill="White" Stretch="Uniform" Width="14" Height="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </Grid>
                </Border>
                
                <!-- Footer -->
                <Button Grid.Row="2" x:Name="BtnClose" Content="닫기" Click="BtnClose_Click" Margin="0,20,0,0" Height="40" Background="#F8F9FA" 
                        Foreground="#555555" BorderBrush="#E0E0E0" BorderThickness="1" FontSize="14" FontWeight="Bold" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="20">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Grid>
    </Border>
</Window>
