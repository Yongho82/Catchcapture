<Window x:Class="CatchCapture.Recording.RecordingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="" 
        SizeToContent="WidthAndHeight"
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="True"
        ResizeMode="NoResize"
        Opacity="0"
        WindowStartupLocation="Manual"
        PreviewKeyDown="Window_PreviewKeyDown">
    
    <Window.Resources>
        <!-- 툴바 버튼 스타일 -->
        <Style x:Key="ToolbarButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#20000000" TargetName="border"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#30000000" TargetName="border"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 토글 버튼 스타일 -->
        <Style x:Key="ToggleToolbarButtonStyle" TargetType="ToggleButton">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#20000000" TargetName="border"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Background" Value="#304361EE" TargetName="border"/>
                                <Setter Property="BorderThickness" Value="1" TargetName="border"/>
                                <Setter Property="BorderBrush" Value="#4361EE" TargetName="border"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- 도구 라디오 버튼 스타일 -->
        <Style x:Key="ToolRadioButtonStyle" TargetType="RadioButton">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#20000000" TargetName="border"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Background" Value="#304361EE" TargetName="border"/>
                                <Setter Property="BorderThickness" Value="1" TargetName="border"/>
                                <Setter Property="BorderBrush" Value="#4361EE" TargetName="border"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 색상 버튼 스타일 -->
        <Style x:Key="ColorButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="20"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="10">
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#888888" TargetName="border"/>
                                <Setter Property="BorderThickness" Value="2" TargetName="border"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 세로 구분선 -->
        <Style x:Key="VerticalSeparatorStyle" TargetType="Separator">
            <Setter Property="Width" Value="1"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Margin" Value="4,0"/>
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Separator">
                        <Rectangle Fill="{TemplateBinding Background}" Width="{TemplateBinding Width}" Height="{TemplateBinding Height}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Border CornerRadius="8" Background="#FFFFFF" BorderThickness="1" BorderBrush="#E0E0E0">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="2" BlurRadius="10" Opacity="0.3" Direction="270"/>
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="28"/> <!-- 타이틀바 -->
                <RowDefinition Height="*"/>  <!-- 툴바 -->
            </Grid.RowDefinitions>
            
            <!-- 타이틀바 (하이그로시 3D 효과) -->
            <Border Grid.Row="0" CornerRadius="8,8,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <!-- 하이그로시 그라데이션 배경 -->
                    <Border CornerRadius="8,8,0,0">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#6E8CF0" Offset="0.0"/> <!-- 상단 하이라이트 -->
                                <GradientStop Color="#4E6ADF" Offset="0.4"/> <!-- 메인 블루 -->
                                <GradientStop Color="#324BB4" Offset="1.0"/> <!-- 하단 쉐도우 -->
                            </LinearGradientBrush>
                        </Border.Background>
                    </Border>
                    
                    <!-- 입체감을 위한 상단 미세 선 -->
                    <Border BorderThickness="0,1,0,0" BorderBrush="#A0FFFFFF" CornerRadius="8,8,0,0" Opacity="0.3"/>
                    
                    <StackPanel Orientation="Horizontal" Margin="10,0,0,0">
                        <Image Source="/icons/videocamera.png" Width="14" Height="14" Margin="0,0,6,0" 
                               RenderOptions.BitmapScalingMode="HighQuality"/>
                        <TextBlock x:Name="TitleText" Foreground="White" FontWeight="SemiBold" FontSize="11"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                    
                    <!-- 닫기 버튼 -->
                    <Button x:Name="CloseButton" HorizontalAlignment="Right" Margin="0,0,8,0"
                            Width="18" Height="18" Background="Transparent" BorderThickness="0"
                            Cursor="Hand" Click="CloseButton_Click">
                        <TextBlock Text="✕" FontSize="10" Foreground="White" 
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Button>
                </Grid>
            </Border>
            
            <!-- 툴바 -->
            <Border Grid.Row="1" Background="#FAFAFA" CornerRadius="0,0,8,8">
                <StackPanel x:Name="MainToolbarPanel" Orientation="Horizontal" Margin="8,4" VerticalAlignment="Center">
                    
                    <!-- 녹화 시간 -->
                    <TextBlock x:Name="RecordingTimeText" Text="00:00" 
                               FontSize="14" FontWeight="SemiBold" 
                               Foreground="#333333" VerticalAlignment="Center"
                               MinWidth="45" Margin="0,0,8,0"/>
                    
                    <!-- 녹화 시작/정지 버튼 -->
                    <Button x:Name="RecordButton" Style="{StaticResource ToolbarButtonStyle}"
                            Click="RecordButton_Click" ToolTip="녹화 시작/정지">
                        <Grid>
                            <Ellipse x:Name="RecordButtonIcon" Width="16" Height="16" Fill="#E53935"/>
                            <Rectangle x:Name="StopButtonIcon" Width="12" Height="12" Fill="#333333" Visibility="Collapsed"/>
                        </Grid>
                    </Button>
                    
                    <!-- 일시정지/재개 버튼 (녹화 중에만 표시) -->
                    <Button x:Name="PauseButton" Style="{StaticResource ToolbarButtonStyle}"
                            Click="PauseButton_Click" ToolTip="일시정지" Visibility="Collapsed" Margin="4,0,0,0">
                        <Grid>
                            <!-- 일시정지 아이콘 (||) -->
                            <StackPanel x:Name="PauseIcon" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Rectangle Width="4" Height="14" Fill="#333333" Margin="0,0,2,0" RadiusX="1" RadiusY="1"/>
                                <Rectangle Width="4" Height="14" Fill="#333333" Margin="2,0,0,0" RadiusX="1" RadiusY="1"/>
                            </StackPanel>
                            <!-- 재개 아이콘 (▶) -->
                            <TextBlock x:Name="ResumeIcon" Text="▶" FontSize="14" FontWeight="Bold" Foreground="#4361EE" 
                                     Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="2,0,0,0"/>
                        </Grid>
                    </Button>
                    
                    <!-- 가변 설정 패널 (접기/펴기 대상) -->
                    <StackPanel x:Name="SettingsPanel" Orientation="Horizontal">
                        <Separator x:Name="Separator1" Width="1" Height="20" Margin="4,0" Background="#E0E0E0"/>
                        
                        <!-- 시스템 오디오 토글 (기본 OFF) -->
                        <ToggleButton x:Name="SystemAudioButton" Style="{StaticResource ToggleToolbarButtonStyle}"
                                Click="SystemAudioButton_Click" ToolTip="시스템 소리 녹음 (OFF)">
                            <Grid>
                                <!-- OFF 상태: 스피커 음소거 아이콘 -->
                                <TextBlock x:Name="SystemAudioOffIcon" Text="🔇" FontSize="14" 
                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                <!-- ON 상태: 스피커 아이콘 -->
                                <TextBlock x:Name="SystemAudioOnIcon" Text="🔊" FontSize="14" 
                                           HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"/>
                            </Grid>
                        </ToggleButton>
                        
                        <!-- 마이크 토글 (기본 OFF) -->
                        <ToggleButton x:Name="MicButton" Style="{StaticResource ToggleToolbarButtonStyle}"
                                Click="MicButton_Click" ToolTip="마이크 녹음 (OFF)">
                            <Grid>
                                <!-- OFF 상태: 마이크 음소거 아이콘 -->
                                <TextBlock x:Name="MicOffIcon" Text="🎤" FontSize="14" Foreground="#999"
                                           HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock.Effect>
                                        <BlurEffect Radius="0.5"/>
                                    </TextBlock.Effect>
                                </TextBlock>
                                <!-- ON 상태: 마이크 활성 아이콘 -->
                                <TextBlock x:Name="MicOnIcon" Text="🎤" FontSize="14" Foreground="#E53935"
                                           HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"/>
                                <!-- 사선 표시 (OFF 상태) -->
                                <Line x:Name="MicSlash" X1="0" Y1="0" X2="16" Y2="16" 
                                      Stroke="#666" StrokeThickness="2" 
                                      HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Grid>
                        </ToggleButton>
                        

                        <Separator x:Name="FormatSeparator" Width="1" Height="20" Margin="4,0" Background="#E0E0E0"/>
                        
                        <!-- 파일 형식 (MP4, GIF, MP3) -->
                        <RadioButton x:Name="RbFormatMp4" Style="{StaticResource ToolRadioButtonStyle}"
                                   GroupName="FormatGroup" Click="FormatRadioButton_Click" ToolTip="MP4 동영상 녹화">
                            <TextBlock Text="MP4" FontSize="9" FontWeight="Bold" Foreground="#4361EE" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </RadioButton>

                        <RadioButton x:Name="RbFormatGif" Style="{StaticResource ToolRadioButtonStyle}"
                                   GroupName="FormatGroup" Click="FormatRadioButton_Click" ToolTip="GIF 애니메이션 녹화">
                            <TextBlock Text="GIF" FontSize="9" FontWeight="Bold" Foreground="#4361EE" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </RadioButton>

                        <RadioButton x:Name="RbFormatMp3" Style="{StaticResource ToolRadioButtonStyle}"
                                   GroupName="FormatGroup" Click="FormatRadioButton_Click" ToolTip="MP3 오디오 전용 녹화">
                            <TextBlock Text="MP3" FontSize="9" FontWeight="Bold" Foreground="#E53935" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </RadioButton>
                        
                        <!-- 화질 설정 -->
                        <Button x:Name="QualityButton" Style="{StaticResource ToolbarButtonStyle}"
                                Click="QualityButton_Click" ToolTip="화질 (HD/SD/LD)">
                            <TextBlock x:Name="QualityText" Text="HD" FontSize="11" FontWeight="Bold" Foreground="#333"/>
                        </Button>
                        
                        <!-- 프레임 설정 -->
                        <Button x:Name="FpsButton" Style="{StaticResource ToolbarButtonStyle}"
                                Click="FpsButton_Click" ToolTip="프레임 레이트">
                            <TextBlock x:Name="FpsText" Text="30F" FontSize="11" FontWeight="Bold" Foreground="#333"/>
                        </Button>
                        
                        <!-- 마우스 효과 -->
                        <ToggleButton x:Name="MouseEffectToggle" Style="{StaticResource ToggleToolbarButtonStyle}"
                                      Click="MouseEffectToggle_Click" ToolTip="마우스 커서 표시">
                            <Grid>
                                <TextBlock x:Name="MouseOnIcon" Text="🖱️" FontSize="14" Visibility="Collapsed"/>
                                <Grid x:Name="MouseOffIcon">
                                    <TextBlock Text="🖱️" FontSize="14" Opacity="0.5"/>
                                    <Line X1="0" Y1="0" X2="16" Y2="16" Stroke="#666" StrokeThickness="2" 
                                          HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Grid>
                            </Grid>
                        </ToggleButton>
                        
                        <!-- 카운트다운 -->
                        <Button x:Name="TimerButton" Style="{StaticResource ToolbarButtonStyle}"
                                Click="TimerButton_Click" ToolTip="시작 전 카운트다운">
                            <Grid>
                                <TextBlock Text="⏱️" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                <Border x:Name="TimerCountBadge" Background="#E53935" CornerRadius="7" MinWidth="14" Height="14" 
                                        HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,-4,-4"
                                        Visibility="Visible">
                                    <TextBlock x:Name="TimerText" Text="3" FontSize="10" FontWeight="Bold" Foreground="White"
                                               HorizontalAlignment="Center" VerticalAlignment="Center" Margin="2,-1,2,0"/>
                                </Border>
                            </Grid>
                        </Button>
                        
                        <Separator x:Name="Separator2" Width="1" Height="20" Margin="4,0" Background="#E0E0E0"/>
                        
                        <!-- 영역 새로 선택 -->
                        <ToggleButton x:Name="AreaSelectButton" Style="{StaticResource ToggleToolbarButtonStyle}"
                                      Click="AreaSelectButton_Click" ToolTip="영역 선택">
                            <TextBlock Text="⛶" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </ToggleButton>
                        

                        
                        <!-- 전체 화면 -->
                        <Button x:Name="FullScreenButton" Style="{StaticResource ToolbarButtonStyle}"
                                Click="FullScreenButton_Click" ToolTip="전체 화면">
                            <TextBlock Text="🔲" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Button>
                        
                        <!-- 그리기 도구 -->
                        <ToggleButton x:Name="DrawingButton" Style="{StaticResource ToggleToolbarButtonStyle}"
                                      Click="DrawingButton_Click" ToolTip="그리기 도구">
                            <TextBlock Text="✏️" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </ToggleButton>
                        
                        <!-- 영역 크기 -->
                        <TextBlock x:Name="AreaSizeText" Text="800x600" 
                                   FontSize="11" Foreground="#666666" 
                                   VerticalAlignment="Center" Margin="4,0"/>
                    </StackPanel>

                    <!-- 접기/펴기 버튼 -->
                    <Button x:Name="CollapseButton" Margin="4,0,0,0"
                            Width="20" Height="24" Background="Transparent" BorderThickness="0"
                            Cursor="Hand" Click="CollapseButton_Click" ToolTip="도구 모음 숨기기">
                        <TextBlock x:Name="CollapseArrow" Text="◀" FontSize="10" Foreground="#666" FontWeight="Bold"/>
                    </Button>
                </StackPanel>
            </Border>
            
            <!-- 그리기 도구 팝업 -->
            <Popup x:Name="DrawingPopup" PlacementTarget="{Binding ElementName=DrawingButton}" 
                   Placement="Bottom" StaysOpen="True" AllowsTransparency="True"
                   PopupAnimation="Slide" Closed="DrawingPopup_Closed" Opened="DrawingPopup_Opened">
                <Border Background="White" BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="4" Padding="5" Margin="0,5,0,0">
                    <Border.Effect>
                        <DropShadowEffect ShadowDepth="2" BlurRadius="5" Opacity="0.2"/>
                    </Border.Effect>
                    <StackPanel Orientation="Horizontal">
                        <!-- 펜 -->
                        <RadioButton x:Name="PenRadio" GroupName="DrawingTool" IsChecked="True" 
                                     Style="{StaticResource ToolRadioButtonStyle}" Click="PenRadio_Click" ToolTip="펜">
                            <TextBlock Text="🖊️" FontSize="16"/>
                        </RadioButton>
                        <!-- 형광펜 -->
                        <RadioButton x:Name="HighlightRadio" GroupName="DrawingTool" 
                                     Style="{StaticResource ToolRadioButtonStyle}" Click="HighlightRadio_Click" ToolTip="형광펜">
                            <TextBlock Text="🖍️" FontSize="16"/>
                        </RadioButton>
                        <!-- 지우개 -->
                        <RadioButton x:Name="EraserRadio" GroupName="DrawingTool" 
                                     Style="{StaticResource ToolRadioButtonStyle}" Click="EraserRadio_Click" ToolTip="지우개">
                            <TextBlock Text="🧹" FontSize="16"/>
                        </RadioButton>
                        
                        <Separator Style="{StaticResource VerticalSeparatorStyle}"/>
                        
                        <!-- 직선 -->
                        <RadioButton x:Name="LineRadio" GroupName="DrawingTool" 
                                     Style="{StaticResource ToolRadioButtonStyle}" Click="LineRadio_Click" ToolTip="직선">
                            <TextBlock Text="📏" FontSize="16"/>
                        </RadioButton>
                        <!-- 화살표 -->
                        <RadioButton x:Name="ArrowRadio" GroupName="DrawingTool" 
                                     Style="{StaticResource ToolRadioButtonStyle}" Click="ArrowRadio_Click" ToolTip="화살표">
                            <TextBlock Text="➡️" FontSize="16"/>
                        </RadioButton>
                        <!-- 사각형 -->
                        <RadioButton x:Name="RectangleRadio" GroupName="DrawingTool" 
                                     Style="{StaticResource ToolRadioButtonStyle}" Click="RectangleRadio_Click" ToolTip="사각형">
                            <TextBlock Text="⬜" FontSize="16"/>
                        </RadioButton>
                        <!-- 원 -->
                        <RadioButton x:Name="EllipseRadio" GroupName="DrawingTool" 
                                     Style="{StaticResource ToolRadioButtonStyle}" Click="EllipseRadio_Click" ToolTip="원">
                            <TextBlock Text="⭕" FontSize="16"/>
                        </RadioButton>
                        <!-- 번호 매기기 -->
                        <RadioButton x:Name="NumberingRadio" GroupName="DrawingTool" 
                                     Style="{StaticResource ToolRadioButtonStyle}" Click="NumberingRadio_Click" ToolTip="번호 매기기">
                            <TextBlock Text="❶" FontSize="16"/>
                        </RadioButton>
                        
                        <Separator Style="{StaticResource VerticalSeparatorStyle}"/>
                        
                        <!-- 색상 선택 -->
                        <Button x:Name="ColorRed" Style="{StaticResource ColorButtonStyle}" Background="Red" Click="ColorButton_Click" Tag="#FFFF0000"/>
                        <Button x:Name="ColorYellow" Style="{StaticResource ColorButtonStyle}" Background="Yellow" Click="ColorButton_Click" Tag="#FFFFFF00"/>
                        <Button x:Name="ColorGreen" Style="{StaticResource ColorButtonStyle}" Background="Lime" Click="ColorButton_Click" Tag="#FF00FF00"/>
                        <Button x:Name="ColorBlue" Style="{StaticResource ColorButtonStyle}" Background="Blue" Click="ColorButton_Click" Tag="#FF0000FF"/>
                        <Button x:Name="ColorBlack" Style="{StaticResource ColorButtonStyle}" Background="Black" Click="ColorButton_Click" Tag="#FF000000"/>
                        <Button x:Name="ColorWhite" Style="{StaticResource ColorButtonStyle}" Background="White" Click="ColorButton_Click" Tag="#FFFFFFFF"/>
                        
                        <Separator Style="{StaticResource VerticalSeparatorStyle}"/>
                        
                        <!-- 전체 지우기 -->
                        <Button x:Name="ClearAllButton" Style="{StaticResource ToolbarButtonStyle}" Width="24" Height="24" 
                                Click="ClearAllButton_Click" ToolTip="모두 지우기">
                            <TextBlock Text="🗑️" FontSize="12"/>
                        </Button>
                    </StackPanel>
                </Border>
            </Popup>
        </Grid>
    </Border>
</Window>
